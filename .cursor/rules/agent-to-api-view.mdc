---
description: 联调 View：AppLoadingContainer（普通列表 + Table asTable）、loading error empty submitting、TanStack useReactTable、formatDateFromMillisecond、formatNumber、枚举映射 t()；仅视觉还原、不接表格数据态时不要启用。
globs: "**/*.tsx"
alwaysApply: false
---

# Agent 到 API · View（展示层）

负责**用户最终看到什么**。展示组件优先接受已归一化数据，禁止散落请求逻辑；状态、表格、格式化都按下文统一约定落位。

## 1) 列表三态容器：`AppLoadingContainer`（强制）

联调时**主列表 / 主内容区**的首屏 `loading` / `error` / `empty` **必须**通过 **`@/components/common/AppLoadingContainer`** 统一承载，**禁止**在业务组件内手写 `Spinner` + 独立 `section`/`li` 分支拼装三态（与容器内置 Spinner、空态图、`t('加载失败')` 重复且样式漂移）。

### 1.1 普通列表（卡片网格、`ul`/`ol`、块级列表等）

- 用 `AppLoadingContainer` **包裹**有数据时的列表子树（`ul`、网格 `section`、`map` 渲染区等）。
- 推荐显式传参（与 TanStack Query 语义对齐）：
  - `data={rows}`：稳定为 `readonly T[]`；首屏加载中可传 `[]`。
  - `isLoading={query.isLoading}`（或等价首屏 loading 标志）。
  - `isError={query.isError}`（主列表须保留；**内嵌摘录卡**见 §1.3）。
  - `emptyDescription={t('…')}`：按业务覆盖默认「暂无数据」（如 `t('暂无短剧')`、`t('未连接钱包')`）。
  - `minHeight`：列表区建议 `280` 或与稿面主列表最小高度一致，避免三态区域过扁。
- **翻页 / 加载更多**（`fetchNextPage`、`loadMore`、哨兵触底）的**次级** loading 仍可在列表**底部**保留内联 `Spinner` 或「加载更多」按钮；**不得**用底部 Spinner 替代首屏 `AppLoadingContainer`。
- 参考：`src/features/invite/components/InviteRecordsCard.tsx`、`src/features/narrator/components/DramaManagementPanel.tsx`、`src/features/mining/components/MiningEarningsSection.tsx`。

```tsx
<AppLoadingContainer
  data={filteredRows}
  isLoading={isListLoading}
  isError={isListError}
  minHeight={280}
  emptyDescription={t('暂无短剧')}
>
  <section className={CARD_GRID_CLASS}>
    {filteredRows.map((row) => (
      <article key={row.id}>{/* … */}</article>
    ))}
  </section>
</AppLoadingContainer>
```

### 1.2 数据表（`Table` + `TableBody`）

- 首屏三态**必须**在 **`TableBody` 内**使用同一组件，并开启表格模式：
  - `asTable`
  - `colSpan={columnCount}`（与 `columns.length` / `table.getVisibleLeafColumns().length` 一致，覆盖整表宽度）。
  - `data={rowModelRows}`（或等价行数组）、`isLoading`、`isError`；空表走容器默认 `t('暂无记录')` 或 `emptyDescription`。
- `children` **仅**渲染数据行（`TableRow` + `TableCell` + `flexRender`）；**禁止**再手写三态 `TableRow`。
- 表头 `TableHeader` 保持在 `AppLoadingContainer` **之外**，加载中仍展示表头结构。
- 参考：`src/features/history/components/HistoryTransactionsTable.tsx`、`src/features/income/components/IncomeHistoryTable.tsx`。

```tsx
<TableBody>
  <AppLoadingContainer
    asTable
    colSpan={columnCount}
    data={rowModelRows}
    isLoading={isLoading}
    isError={isError}
  >
    {rowModelRows.map((row) => (
      <TableRow key={row.id}>{/* flexRender cells */}</TableRow>
    ))}
  </AppLoadingContainer>
</TableBody>
```

### 1.3 通用状态落位（loading / error / empty / submitting）

- **主列表 / 主内容区**（整页数据表、独立子页列表、占据版心主体的区块等）联调时须显式覆盖 `loading` / `error` / `empty` / `submitting` 四类状态；只实现 happy path 视为未完成。
- **内嵌摘录卡**（聚合页中仅展示少量条目的卡片模块，如邀请首页「邀请记录」预览卡）：卡片内仍用 `AppLoadingContainer` 覆盖 `loading` / `empty`；**不在卡片内单独渲染 `加载失败` 行**——`error` 由 **`appAxiosInstance` 等业务层统一 toast / 全局反馈**承接（`isError` 可不传或传 `false`）。参考 `InviteRecordsCard.tsx`。
- 状态文案由 `AppLoadingContainer` 内置 i18n（`t('加载失败')`、默认空态 `t('暂无数据')` / `t('暂无记录')`）；业务侧通过 `emptyDescription` 覆盖空态文案。`submitting` 仍走 toast / 按钮禁用等，不由本容器承担。
- 三态判定顺序由容器保证：`isLoading -> isError -> 空列表 -> children`；业务层勿再包一层同序分支。
- 兼容旧写法：`data={null}` 且未传 `isLoading` 时视为加载中；**新联调代码优先** `isLoading` + `data={[]}` 或真实数组。
- 决定首屏 loading 标志的责任在**容器层**（详见 **`@.cursor/rules/agent-to-api-controller.mdc`** 容器/展示分工）。

## 2) 表格型状态落位（强制，与 §1.2 一致）

- 数据表首屏 `loading` / `error` / `empty` **只**允许通过 **`AppLoadingContainer` + `asTable` + `colSpan`** 落在 `TableBody` 内（见 §1.2）；**禁止**手写三态 `TableRow + TableCell` 或把状态渲染到 `Table` 外。
- 须配合 **§3** 的 shadcn `Table` + TanStack `useReactTable` 列模型；`colSpan` 与当前可见列数一致。

## 3) 历史流水类表格实现约定（推荐升级为默认）

- 表格实现默认采用 **shadcn `Table` 原语 + TanStack `useReactTable`**：列结构使用 `ColumnDef<T>[]`，渲染使用 `flexRender`，基础 row model 使用 `getCoreRowModel`。
- `columns` 建议使用 `useMemo` 包裹，并将 `t` 纳入依赖，避免语言切换后表头与单元格文案不更新。
- 行唯一键（`getRowId`）优先复用后端稳定字段（如订单号 + 时间戳）；仅在字段缺失时回退索引，减少 key 抖动造成的重渲染。
- 数量列遵循「符号与格式分离」：符号（`+/-`）由业务规则决定，绝对值交给 `formatNumber` 处理；禁止把符号逻辑耦合进数字格式化工具。

## 4) 日期时间格式化（强制）

- 联调真实接口时，凡 UI 出现日期/时间字段，必须复用项目日期工具：`@/utils/formatDate`；禁止在业务组件里手写 `new Date(...).toLocaleString()`、`dayjs(...).format(...)` 或字符串拼接格式化。
- 历史记录等联调表格中的时间展示统一使用 `formatDateFromMillisecond`。
- 数据展示层不需要额外中间格式化方法（如 `formatXxxForView`）；应在 JSX/DOM 渲染位置直接调用格式化函数。

## 5) 数字展示格式化（强制）

- 联调真实接口时，凡 UI 出现金额/数量/余额等数字字段，必须复用项目数字工具：`@/utils/formatNumber`（`formatNumber`）；禁止在业务组件里直接 `Number(...).toFixed(...)`、`toLocaleString()` 或字符串拼接千分位。
- 数值正负号展示由业务层控制（如 `+` / `-`），但主体数字格式（精度、千分位、极小值显示）必须交由 `formatNumber` 统一处理。
- 数据展示层不需要额外中间格式化方法；应在 JSX/DOM 渲染位置直接调用 `formatNumber`。
- 禁止为一次性展示字段新增"中间态格式化函数"（如 `const formatCount = (...) => ...` 后仅在本组件 1-2 处透传使用）；优先在渲染处直接表达「判空 + formatNumber + 占位符」。

> **配套约束**：浮点计算与派生（`mathUtil` / 派生值 `undefined` 语义）属于 Model 层，详见 **`@.cursor/rules/agent-to-api-model.mdc`**「数值计算工具」与「派生值空态语义」。

## 6) 展示格式化位置（强制）

- 仅用于展示的格式化表达（金额、余额、百分比、日期字符串等）必须放在**展示层渲染位置**（JSX/DOM）调用，不得提前下沉到逻辑层做"展示专用"中间变量。
- 若同一格式化表达在一个组件内复用次数较少（1~2 处），优先直接在对应 JSX 处内联调用工具方法（如 `formatNumber`、`toPercent`、`formatDateFromMillisecond`）。
- 仅当同一展示格式化表达复用频次较高（≥3 处）或表达式显著影响可读性时，才允许抽出局部变量；抽出时需在定义处用中文注释说明该变量是"展示口径复用"，禁止混入业务语义计算。

## 7) 枚举值与展示文案映射（强制）

- 后端枚举码值（如 `bizType`）进入展示层前，必须通过**可维护的映射表/函数**做语义化转换（例如 `Record<string, string> + getXxxKey()`），禁止在 JSX 内散落多段三元表达式硬翻译。
- 枚举映射结果应继续走 `t(...)` 输出，保证多语言一致；当码值缺失或映射未命中时，允许按「备注字段 -> 原码值 -> '-'」这类有序兜底，禁止静默吞值。
