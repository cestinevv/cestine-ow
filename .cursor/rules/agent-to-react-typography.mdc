---
description: 字体与排版 — 高保真数值、移动端缩放 0.88、字号上下限与间距同步（尺寸验收见 agent-to-react-sizing）
globs: "**/*.tsx"
alwaysApply: true
---

# 字体与排版（Typography）

稿面尺寸对齐、断点一致性与提交前自检见 **`@.cursor/rules/agent-to-react-sizing.mdc`**。

- **使用 Tailwind 类名**：直接使用 Tailwind 的文本类名（如 `text-sm`, `text-base`, `text-lg`, `text-2xl`, `font-bold` 等）设置字号或字体。
- 字符间距等视觉微调（如 `tracking-tight`, `tracking-[-0.68px]`）也通过 Tailwind 类实现。
- **高保真优先级（强制）**：当 Figma 已明确给出 `font-size / line-height / letter-spacing` 数值时，必须优先按数值还原，不允许为了“视觉层级更好看”主动改大或改小字号。
  - 字号映射优先顺序：`标准 text-*`（能精确命中时） > `text-[Npx]`（无法精确命中时）。
  - 行高映射优先顺序：`leading-*`（能精确命中时） > `leading-[N]`。
  - 字距映射优先顺序：`tracking-*`（能精确命中时） > `tracking-[Npx]`。
  - 只有在用户明确要求“按工程规范重构，不做 1:1 还原”时，才允许偏离上述数值。
- **移动端字号统一缩放（新增强制）**：当用户明确“移动端无原稿，按规则适配”时，`<md` 的字号需在桌面稿基础上按**统一比例**整体缩小，默认推荐比例为 **0.88（约缩小 12%）**。
  - **统一性要求**：同一页面/同一模块群必须复用同一个缩放系数，禁止 A 模块缩 10%、B 模块缩 20% 的随机缩放。
  - **作用范围**：缩放优先作用于 `font-size`，并按原层级关系同步微调 `line-height/letter-spacing` 以保持专业与舒适的阅读节奏。
  - **边界约束**：仅允许作用于 `<md`；`md` 及以上必须保持桌面 Figma 数值，不得被移动端策略污染。换算后的 `<md` 字号还须同时满足下文 **12px 下限** 与 **36px 上限**。
  - **可调区间**：如视觉密度特殊，可在 **0.85~0.90** 内统一调整，但一次任务内必须固定为单一比例并在回复中说明所用系数。
- **移动端字号下限（强制）**：任何用户可见的正文/标签/按钮文案，`font-size` **不得低于 12px**。若按比例缩小后小于 12px，**一律钳制为 12px**（优先使用 `text-xs` 或 `text-[12px]`）。禁止为“塞进小圆角”使用 `text-[10px]` / `text-[11px]` 等。
- **移动端字号上限（强制）**：当「移动端无原稿，按规则适配」时，凡作用在 `<md` 的用户可见文案，`font-size` **不得大于 36px**。若按系数计算或从桌面样式继承会得到更大字号，**一律钳制为 36px**（优先 `text-[36px]`，或与 Tailwind 刻度一致时可用 `text-4xl`）；**含首屏 Hero 主标题在内**均不得突破；`md` 及以上保持桌面 Figma 数值，不受此上限约束。
- **移动端间距统一缩放（新增强制）**：与字号策略一致，当“移动端无原稿，按规则适配”时，`<md` 的 **`padding / margin / gap`（以及为可读性服务的 `min-height`）** 以桌面稿（`md` 侧）为基准，按**同一缩放系数（默认 0.88，与字号同档）**计算目标像素后，**必须仍用本项目的 4px 间距刻度书写**（`1` = 4px 的标准 `p-* / m-* / gap-* / space-*` 等），**禁止**为凑 0.88 精确值而写 `gap-[22px]` 这类布局任意值。
  - **落地算法（强制）**：先算 `目标 px ≈ 桌面 px × 0.88`，再**四舍五入到最近的 4px 倍数**，映射为标准 Tailwind 间距类；若落在两档之间导致观感略紧/略松，允许在 **±1 个间距档位**内做兼容（仍必须是 4px 倍数），并在同一页面保持同一套取舍习惯。
  - **边界约束**：仅作用于 `<md`；`md` 及以上保持桌面 Figma 间距，不得被移动端策略污染。
