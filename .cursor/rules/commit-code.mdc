---
description: 用户要求提交/推送代码时的默认工作流（feat 中文 commit、ci_acp.sh）
alwaysApply: true
---

# 提交代码（Commit Code）

## 触发

用户说「提交代码」「提交并推送」「执行 ci_acp.sh」「commit code」，或明确要求 `feat: xxx` 格式时，按本节执行。

**仅在被明确要求时提交**；不要主动 commit / push。

## 提交信息

必须使用中文简要总结：

```text
feat: 中文简要总结说明
```

- 用户已给出总结 → 直接 `feat: 用户总结`
- 未给出 → 根据本次改动概括后再写 `feat: …`
- 保持简短，聚焦目的

## 工作流

1. 并行执行：`git status`、`git diff`、`git log -5 --oneline`，确认待提交范围与仓库 commit 风格。
2. 检查项目根是否存在 `ci_acp.sh`；若不存在则创建（内容固定如下，勿擅自改写已有脚本）：

```bash
git add ./
git status
git commit -m "$1"
git pull --rebase
git push
```

3. 提交前确认信息符合 `feat: 中文简要总结说明`；**禁止**提交 `.env`、密钥、凭据；若 `git add ./` 会纳入这类文件，先停止并提醒用户。
4. 执行：

```bash
bash ./ci_acp.sh "feat: 中文简要总结说明"
```

5. 结束后简要说明结果；失败时说明失败步骤与终端关键错误。

## 注意

- `ci_acp.sh` 会 `git add ./`，提交前须提醒用户该行为。
- 若用户**只要求 commit 不要求 push**，勿执行脚本中的 `git push`；可改用手动 `git add`（限定文件）+ `git commit`，不跑完整 `ci_acp.sh`。
- 若 `git commit` 被 hook 拒绝，修复后**新建 commit**，不要 amend（除非用户明确要求且 HEAD 未推送）。
