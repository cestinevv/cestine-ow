/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  type Codec,
  combineCodec,
  type Decoder,
  type Encoder,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/**
 * Parameters shared by all delegator-signed instructions:
 * `mint_series_nft`, `batch_mint_actor_nft`, `upgrade_actor_nft`,
 * `refill_actor_stamina`, `batch_refill_actor_stamina`.
 *
 * `canonical_payload` is a pipe-delimited UTF-8 string whose format
 * depends on the instruction (see each handler's doc comment).
 *
 * `sig` is the 64-byte Ed25519 signature the backend produces over
 * SHA256(canonical_payload).
 */
export type SignedParams = {
  canonicalPayload: string;
  sig: ReadonlyUint8Array;
};

export type SignedParamsArgs = SignedParams;

export function getSignedParamsEncoder(): Encoder<SignedParamsArgs> {
  return getStructEncoder([
    [
      "canonicalPayload",
      addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
    ],
    ["sig", fixEncoderSize(getBytesEncoder(), 64)],
  ]);
}

export function getSignedParamsDecoder(): Decoder<SignedParams> {
  return getStructDecoder([
    [
      "canonicalPayload",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ["sig", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

export function getSignedParamsCodec(): Codec<SignedParamsArgs, SignedParams> {
  return combineCodec(getSignedParamsEncoder(), getSignedParamsDecoder());
}
