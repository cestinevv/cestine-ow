---
description: 栅格图片 — Figma 导出 2×、落盘项目位图目录、JPG/PNG 选型
globs: "**/*.tsx"
alwaysApply: true
---

# 栅格图片（Photos / Bitmaps）

与 **矢量图标** 的分流见 **`@.cursor/rules/agent-to-react-icons.mdc`**：先在稿面判断节点是 **Icon（矢量 UI 符号）** 还是 **栅格图（照片、插画位图、大背景等）**；**Icon 一律走 Icon 规则**，本文件仅约束 **位图资源**。

- **落盘目录**：导出文件写入**项目约定的位图资源目录**（编码前检索现有 `import ... from '...image...'` 或 `assets`、`public` 等实际用法，**与之对齐**），命名建议 kebab-case（如 `hero-banner.png`）。
- **导出尺寸（2×）**：以设计稿中该资源在对应 frame（桌面 / 移动端以交付目标为准）下的 **逻辑像素宽高**为基准，导出 **宽、高各 ×2** 的位图（Retina 密度），代码侧仍按设计稿 **1×** 数值写 `width`/`height` 或布局，由浏览器缩放显示。
- **格式选择**：
  - **JPG / JPEG**：画面 **无** 需保留的 **半透明** 或 **全透明** 通道（无实质 alpha / 无镂空）时使用，以控制体积。
  - **PNG**：存在 **半透明**、**全透明**、镂空、需无损边缘或与透明背景叠加时使用。

- **禁止**：将 Figma MCP 返回的临时资源 URL 当作长期 `src`；应导出并落盘后再通过 **与仓库一致的 import 路径 / 别名** 引用（与现有图片 import 写法保持一致）。
